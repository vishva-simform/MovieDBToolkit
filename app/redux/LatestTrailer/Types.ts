import type { ResultType } from '../../components';

interface LatestTrailerStateType {
  latestTrailerList: Array<ResultType>;
  latestTrailerPage?: number;
  status: string;
}

export type { LatestTrailerStateType };
